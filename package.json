{
  "name": "it-academy",
  "version": "1.0.0",
  "scripts": {
    "lint": "npm run lint-html && npm run lint-css && npm run lint-js",
    "lint-html": "linthtml src/**/*.html",
    "lint-css": "stylelint **/*.css",
    "lint-js-all": "eslint src/",
    "lint-js-diff": "DIFF=$(git diff --diff-filter=ACM --name-only origin/master | grep '.js$' | xargs) && eslint -- $DIFF"
  },
  "devDependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "eslint": "^8.6.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.25.4"
  }
}
